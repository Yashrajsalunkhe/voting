{
  "name": "aisa-voting-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "cd server && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:full": "cd server && npm run dev & cd frontend && npm run dev",
    "build": "npm run install:deps && npm run build:frontend && npm run build:server",
    "install:deps": "cd frontend && npm install && cd ../server && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:server": "cd server && npm run build",
    "start": "cd server && npm start",
    "clean": "cd server && npm run clean && cd ../frontend && rm -rf dist",
    "vercel-build": "npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}